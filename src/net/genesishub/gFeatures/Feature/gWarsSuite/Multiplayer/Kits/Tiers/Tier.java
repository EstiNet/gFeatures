package net.genesishub.gFeatures.Feature.gWarsSuite.Multiplayer.Kits.Tiers;

import org.bukkit.entity.Player;

import net.genesishub.gFeatures.Feature.gWarsSuite.Multiplayer.Kits.*;

public class Tier <T>{
	/*TierZero tz = new TierZero();
	TierOne to = new TierOne();
	TierTwo tt = new TierTwo();
	TierThree tth = new TierThree();
	TierFour tf = new TierFour();
	public void sort(T tier, Player p){
		if(tier.getClass().getName().equalsIgnoreCase("ZeroTier")){
			ZeroTier zt = (ZeroTier) tier;
			tz.initialize(zt.toString(), p);
		}
		else if(tier.getClass().getName().equalsIgnoreCase("OneTier")){
			OneTier zt = (OneTier) tier;
			to.initialize(zt.toString(), p);
		}
		else if(tier.getClass().getName().equalsIgnoreCase("TwoTier")){
			TwoTier zt = (TwoTier) tier;
			tt.initialize(zt.toString(), p);
		}
		else if(tier.getClass().getName().equalsIgnoreCase("ThreeTier")){
			ThreeTier zt = (ThreeTier) tier;
			tth.initialize(zt.toString(), p);
		}
		else if(tier.getClass().getName().equalsIgnoreCase("FourTier")){
			FourTier zt = (FourTier) tier;
			tf.initialize(zt.toString(), p);
		}
	}*/
}
